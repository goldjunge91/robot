joy_node:
  ros__parameters:
    dev: "/dev/input/js0"
    deadzone: 0.1
    autorepeat_rate: 20.0
    coalesce_interval_ms: 10
    
    # Example mapping for an Xbox controller. Adjust based on your jstest results!
    # ax_x_mapping: # -1.0 to 1.0 (left to right)
    # ax_y_mapping: # -1.0 to 1.0 (up to down)
    # bt_a_mapping: # button A
    # bt_b_mapping: # button B